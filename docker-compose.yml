services:
  ollama:
    image: ollama/ollama
    container_name: ollama-server
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama

  app:
    build: .
    container_name: my-python-app
    volumes:
      - .:/app
    depends_on:
      - ollama

volumes:
  ollama-data:
